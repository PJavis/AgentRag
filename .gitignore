.env
*.pyc
.cache
.run/
data/
!data/ontology/
!data/ontology/**
# Finetune artifacts (large)
models/
data/finetune/
vendor/llama.cpp/
.superpowers/
image.png
.deepeval/

# Probe row dumps carry corpus text — keep out of git
docs/eval/rows_*.jsonl

# Retrained reranker candidates (large; keep out of git)
models/agentrag-rerank-*/

# Local-dev compose override (see docker-compose.override.yml.example).
# Must never be committed: it is auto-merged into bare `docker compose` runs.
docker-compose.override.yml
